---
id: DA-20260422-audit-system-phase-5
kind: scenario
priority: high
modality: must
severity: audit-failure
trigger:
  keywords:
    - Phase 5
    - Stale 데이터 정리
    - promotion-log 경량화
    - scriptagent 임시 파일
    - 플래그 파일 정리
    - 5-1
    - 5-2
    - 5-3
when: Phase 3C 완료 후 stale 데이터를 정리할 때
if: 3개월+ 오래된 promotion-log 항목, scriptagent 임시 파일, 잔류 플래그 파일이 존재하는 경우
then: '다음 3개 항목을 수행한다. (1) **promotion-log 경량화 (5-1)**: 3개월+ 오래된 항목을 스캔 — 메모리로 승격된 항목은 삭제, 미승격 중요 항목은 `memory/audit/audit-history.md` 로 이관 후 원본 삭제. (2) **scriptagent 임시 파일 정리 (5-2)**: `D:/projects/button/agent/.secretary/.tmp/` 내 대응 조건이 해소된 stale 메시지 파일 (commit-warn / promo-error-remind 등) 을 삭제한다. (3) **플래그 파일 정리 (5-3)**: `~/.claude/.remote-session` (이전 세션 잔류) + `~/.claude/.compressed-recently-*` (scriptagent 플래그) 존재 시 삭제한다. LOCK_TTL 초과 lock 파일은 자동 정리됨이라 수동 개입은 불필요하다.'
because: promotion-log 가 무한 증가하면 Read 부담이 커진다. 잔류 플래그 파일은 다음 세션의 원격 감지 오작동을 유발한다.
sources:
  - canonical: ~/.claude/docs/verification/audit-system.md §audit-system-phase-5
    section-sha256: PLACEHOLDER_RUN_MIGRATE
    last-verified: '2026-04-22'
depends-on: []
applies-with: []
meta-of: []
version: 3
status: active
effective-from: '2026-04-22T00:00:00.000Z'
last-verified: '2026-04-22T00:00:00.000Z'
utility: 0.7
hit-count: 0
auto-keywords:
  - promotion-log
  - cleanup
  - stale
  - data
  - audit
  - scriptagent
  - temporary
  - files
  - flag
  - memory
  - promotion
  - history
  - 3개월
  - 오래된
  - 데이터
  - 정리
  - 경량화
---

# DA-20260422-audit-system-phase-5

## Signal-to-Action

Phase 5 에서 promotion-log 경량화 → scriptagent 임시 파일 정리 → 플래그 파일 정리 순으로 수행한다. 경량화 결과 (삭제 N건, 이관 N건) 를 Phase 6 보고에 기재한다.

## 적용 사례 (retrospective 연계)

- (md-rewrite 변환 시점 — retrospective 누적 예정)
